var sortingNumbers = require("./sorting-numbers");

sortingNumbers.pushToNumbers(4);
sortingNumbers.pushToNumbers(3);
sortingNumbers.pushToNumbers(2);
sortingNumbers.pushToNumbers(8);


console.log(sortingNumbers.returnFinalNunber());
